# F1.11 步骤 1 — 跳过 grill 判断记录（复用上游需求摘要）

> 本文件由 dd-writing-specs 步骤 1.0 写入。被上游 dd-feature-development-workflow 调用，检测到 `.feature-step0-requirements-summary.md` 存在，跳过 grill，复用上游需求摘要。

## 1.0 跳过判断

**判断结果**：✅ 跳过 grill，复用上游需求摘要

**判断依据**：

```bash
test -f docs/planning/P0/F1/.feature-step0-requirements-summary.md && echo "UPSTREAM_EXISTS"
```

文件存在，且由 dd-feature-development-workflow 步骤 0.4 写入。

## 1.1 上游摘要核对结果

**摘要文件**：`docs/planning/P0/F1/.feature-step0-requirements-summary.md`

**核对维度**：

| 维度 | 上游摘要内容 | 步骤 0 规则 | 是否一致 |
|------|--------------|-------------|---------|
| 功能编号 | F1.11 | F1_ClipMind_设计规范.md 中已规划 | ✅ 一致 |
| 优先级 | P0 | F1.x 主线推进 | ✅ 一致 |
| 文档目录 | `docs/planning/P0/F1/` | docs.md 规则 | ✅ 一致 |
| 文件命名前缀 | `F1.11_菜单栏弹窗双击粘贴_*` | docs.md 规则 | ✅ 一致 |
| 编写顺序 | 需求文档 → 设计文档 → 视觉原型 → 测试用例表 | docs.md 规则 | ✅ 一致 |
| 14 条 AC | AC-F1.11-1 ~ AC-F1.11-14 | F1.9 风格一致 | ✅ 一致 |
| 阶段拆分 | Phase 1-4 | 与上游一致 | ✅ 一致 |
| 架构决策（视图层合并） | 已确认接受 | 与 CODING_STANDARDS.md 模块组织无冲突 | ✅ 一致 |

**冲突检测**：无冲突，所有维度一致。

## 1.2 复用的需求摘要核心内容

直接复用 `.feature-step0-requirements-summary.md` 中已确认的：

- **1. 用户问题和业务目标**：菜单栏 popover 与 F1.9 快速粘贴面板交互对齐 + 视图层合并
- **2. 成功标准**：双击/回车粘贴、方向键导航、Esc 关闭、默认高亮第一行、底部三按钮、视图层合并、复用 PasteCoordinator 等
- **3. 范围边界**：必须做（视图层合并、键盘交互、底部三按钮、PanelClosing 扩展等）/ 明确不做（不改 F1.9 触发逻辑、不改 XCUITest ID、不引入新权限、不改 ClipStore）
- **4. 用户流程**：状态栏入口 / 快捷键入口 / 配置按钮 / 退出按钮 / 查看全部按钮 / Esc
- **5. 数据和接口**：修改 PopoverView/QuickPasteView/StatusItemController 等，新增 bottomBar 组件
- **6. 兼容和迁移**：状态栏点击/快捷键/F1.9 XCUITest/openMainWindow/openSettings 通知
- **7. 14 条 AC**：AC-F1.11-1 ~ AC-F1.11-14
- **8. 阶段拆分**：Phase 1-4
- **9. UI 证据**：XCUITest + 手动 + 截图
- **10. 文档位置与编号**：F1.11 / P0 / docs/planning/P0/F1/

**架构决策（视图层合并）**：PopoverView 与 QuickPasteView 合并为 PastePanelView，由 StatusItemController（NSPopover）和 QuickPastePanelController（NSPanel）分别包装。bottomBar 在 popover 场景显示「查看全部/配置/退出」、在快捷键场景隐藏。用户在步骤 0 需求拷问中连续三次未明确选择，基于其他选择推断为视图层合并，用户在 0.3 出口判定中确认接受。

## 1.3 结论

进入步骤 2：写需求文档。需求文档将基于上述复用的需求摘要编写，遵守 dd-write-requirements 的 12 章节和 P0 铁律（不写代码符号）。
