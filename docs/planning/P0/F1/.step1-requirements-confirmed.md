# F1.10 步骤 1 需求确认（跳过 grill 模式）

> 本文件由 dd-writing-specs 步骤 1 写入，记录跳过 grill 判断结果与上游需求摘要核对情况。结束后由步骤 5.7 清理。

## 1. 跳过判断结果

**判断依据**：检测到 `.feature-step0-requirements-summary.md` 存在于 `docs/planning/P0/F1/` 目录。

**判断结论**：跳过 grill，复用上游需求摘要。

**复用来源**：`docs/planning/P0/F1/.feature-step0-requirements-summary.md`（由 dd-feature-development-workflow 步骤 0.4 写入，归上游工作流清理）。

## 2. 上游需求摘要与步骤 0 一致性核对

| 核对项 | 上游摘要内容 | 步骤 0 提取结果 | 一致性 |
|--------|------------|----------------|--------|
| 功能编号 | F1.10 | F1.10（来自 F1 系列约定） | ✅ 一致 |
| 优先级 | P0 | P0（来自上游摘要，无独立功能列表文件） | ✅ 一致 |
| 文档目录 | `docs/planning/P0/F1/`（与 F1.9 同级） | `docs/planning/P0/F1/`（来自 docs.md 规则） | ✅ 一致 |
| 文件命名前缀 | `F1.10_快捷键默认值修改_*` | `F1.10_快捷键默认值修改_*`（参考 F1.9 命名） | ✅ 一致 |
| history 日志 | `docs/planning/P0/F1/historys/2026-07-25-F1.10-快捷键默认值修改.md` | 同上（来自 docs.md historys/ 规则） | ✅ 一致 |
| 编写顺序 | 需求 → 设计 → 视觉原型 → 测试用例表 | 同上（来自 docs.md 与 dd-writing-specs 流程） | ✅ 一致 |
| AC 数量 | 10 条（AC-F1.10-1 ~ AC-F1.10-10，含 1 条回归保护） | 与 dd-write-requirements AC 编号规范一致 | ✅ 一致 |
| 阶段拆分 | 单 Phase 一次性完成 | 单 Phase（用户已确认） | ✅ 一致 |
| App Store 合规 | 主 Scheme `ClipMind` 仍完全合规 | 与 AGENTS.md 第 10 节一致 | ✅ 一致 |
| 涉及 UI | 设置页 HotkeyRecorder 显示从 `"⌘⇧V"` 改为 `"⌘⇧Space"` | 需要视觉原型 HTML | ✅ 一致 |

**核对结论**：上游需求摘要与步骤 0 提取的规则、功能列表、既有规格文档风格完全一致，无需补充 grill。

## 3. 复用的 10 项需求摘要（来自上游）

### 3.1 用户问题和业务目标
- **问题**：当前默认快捷键 `cmd+shift+v` 与 Paste 等热门剪贴板工具冲突，新用户安装后被劫持，影响首启体验。
- **目标**：将默认快捷键改为 `cmd+shift+space`，避开主流应用冲突，且 space 键易于单手操作和记忆。

### 3.2 成功标准
- 新安装用户默认 `cmd+shift+space` 触发快速粘贴面板
- 老用户升级后自动迁移到新默认值
- `HotkeyFormatter` 支持录制/解析/显示 space 键
- 设置页"重置"按钮重置为新默认值
- 测试常量化，后续修改默认值时只改一处
- 所有 AC 通过 XCTest + 手动验收

### 3.3 范围边界
**必须做**：
- 扩展 `HotkeyFormatter` 支持 space 键（keyCode=49）
- 修改三处默认值（`AppSettings`、`GeneralSettingsView`、`HotkeyRecorder` 重置按钮）
- 老用户迁移逻辑（检测 `cmd+shift+v` 自动迁移）
- 抽取测试常量 `TestHotkeys`（default / legacyDefault / arbitrary）
- 测试调整（1 改 + 8 新增 + 5 fixture 改用 `TestHotkeys.arbitrary`）
- F1.10 规格文档套件（4 篇）
- F1 主设计规范、F1.9 测试用例表、视觉原型 同步更新

**明确不做**：
- 不改快捷键配置 UI（保留现有 HotkeyRecorder）
- 不改 F1.9 触发行为（仍是呼出快速粘贴面板）
- 不引入新的快捷键功能

### 3.4 用户流程
- 新用户：首启 → `hotkey=cmd+shift+space` → 按键呼出面板
- 老用户（旧默认值）：升级首启 → 检测 `hotkey=="cmd+shift+v"` → 自动迁移为 `cmd+shift+space`
- 老用户（自定义值）：升级首启 → 检测非默认值 → 保留不变
- 退出条件：迁移在 AppSettings 初始化时执行一次

### 3.5 数据和接口
**修改**：
- `HotkeyFormatter.keyName`/`keyCode` 增加 `49 ↔ "space"` 映射
- `HotkeyFormatter.display` 增加 `space → "Space"` 大写显示
- `AppSettings.hotkey` 默认值改为 `cmd+shift+space`
- `GeneralSettingsView.@AppStorage("hotkey")` 默认值同步
- `HotkeyRecorder` 重置按钮默认值同步

**新增**：
- `AppSettings.migrateLegacyHotkey()` 迁移方法 + 在 AppSettings 初始化时调用
- `ClipMindTests/TestSupport/TestHotkeys.swift` 测试常量枚举

### 3.6 兼容和迁移
| 场景 | 旧值 | 新值 |
|------|------|------|
| 新用户首启 | 无 | `cmd+shift+space` |
| 老用户升级（旧默认） | `cmd+shift+v` | `cmd+shift+space` |
| 老用户升级（自定义） | 用户值 | 保留不变 |

迁移规则：`AppSettings.init` 时调用 `migrateLegacyHotkey()`，仅当 `hotkey == "cmd+shift+v"` 时改为 `cmd+shift+space` 并持久化。

### 3.7 验收标准（AC）
共 10 条 AC（AC-F1.10-1 ~ AC-F1.10-10），其中 AC-F1.10-10 为回归保护。详见上游摘要第 7 节。

### 3.8 阶段拆分
单 Phase 一次性完成（用户已确认）。

### 3.9 UI 证据
- XCUITest：设置页 HotkeyRecorder 默认显示 `"⌘⇧Space"`
- 手动验收：真实环境按 `cmd+shift+space` 验证快速粘贴面板呼出
- 截图：保存到 `docs/planning/P0/F1/screenshots/`

### 3.10 文档位置与编号
- 功能编号：F1.10
- 优先级：P0
- 文档目录：`docs/planning/P0/F1/`
- 文件命名前缀：`F1.10_快捷键默认值修改_*`
- history 日志：`docs/planning/P0/F1/historys/2026-07-25-F1.10-快捷键默认值修改.md`

## 4. 进入步骤 2

需求摘要已确认完整且与项目规则一致，可直接进入步骤 2（写需求文档），调用 dd-write-requirements skill，按 12 章节结构 + P0 铁律（不写代码符号）编写 `F1.10_快捷键默认值修改_需求文档.md`。

> **注意**：需求文档 P0 铁律要求不出现类名、协议名、方法名、字段名、枚举值、配置键名、并发原语、框架 API、文件路径。上游摘要中的 `HotkeyFormatter`、`AppSettings`、`GeneralSettingsView`、`HotkeyRecorder`、`TestHotkeys` 等代码标识符，在需求文档中需转换为业务术语（如"快捷键格式化工具"、"应用设置"、"通用设置视图"、"快捷键录制器"、"测试常量集"）。代码标识符在设计文档和测试用例表中可以保留（设计文档允许引用模块名/类名，测试用例表允许引用测试文件路径）。
