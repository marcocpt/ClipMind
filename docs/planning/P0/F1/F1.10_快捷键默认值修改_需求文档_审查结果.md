# F1.10 快捷键默认值修改 需求文档 3 子代理审查结果

> 本文件由 dd-writing-specs 步骤 3 写入，记录 3 个维度审查员对 F1.10 需求文档 v1.0 的审查结果。
> 审查对象：`F1.10_快捷键默认值修改_需求文档.md` v1.0
> 规则基准来源：`.trae/rules/docs.md` + `AGENTS.md` + `docs/CODING_STANDARDS.md` + dd-write-requirements 默认 12 章节 + dd-write-design 默认 10 章节
> 审查时间：2026-07-25

---

## 审查员 A：完整性 + 一致性 + docs.md 合规性 + dd-write-requirements P0 铁律合规性

### A.1 完整性检查

**章节完整性**：
- 12 章节全部存在（Background / Problem Statement / Goals / Scope / FR / NFR / Constraints / AC / Out of Scope / Terminology / Decision Freedom / Future Considerations）✅
- 文档头部格式：`> 最后更新：2026-07-25 | 版本：v1.0` ✅
- 文末版本记录：v1.0 表格存在 ✅

**FR 完整性**：
- FR 数量：11 条（FR-001 ~ FR-011）✅
- 每条 FR 含"需求"+"行为"两段式描述 ✅
- FR 编号连续无跳号 ✅

**AC 完整性**：
- AC 数量：10 条（AC-F1.10-1 ~ AC-F1.10-10）✅
- 每条 AC 含 Given/When/Then + 验证方式 ✅
- 含 1 条回归保护 AC（AC-F1.10-10）✅

**FR 到 AC 映射检查**：

| FR 编号 | 对应 AC | 是否有 AC |
|---------|---------|----------|
| FR-001（默认值改为新值） | AC-F1.10-4（默认值断言）、AC-F1.10-8（注册触发，部分） | ✅ 有 |
| FR-002（解析空格键） | AC-F1.10-1 | ✅ 有 |
| FR-003（反向构造空格键） | AC-F1.10-3 | ✅ 有 |
| FR-004（显示空格键） | AC-F1.10-2 | ✅ 有 |
| FR-005（老用户迁移） | AC-F1.10-5 | ✅ 有 |
| FR-006（自定义值保留） | AC-F1.10-6 | ✅ 有 |
| FR-007（重置按钮） | AC-F1.10-7 | ✅ 有 |
| FR-008（默认显示） | AC-F1.10-9 | ✅ 有 |
| FR-009（注册并触发） | AC-F1.10-8 | ✅ 有 |
| **FR-010（测试常量集）** | **无直接 AC** | ⚠️ 建议改进 |
| FR-011（回归保护） | AC-F1.10-10 | ✅ 有 |

> **FR-010 备注**：FR-010 是测试基础设施重构（maintainability FR），非用户可观察行为。dd-write-requirements P1 规则"每个 FR 至少一个 AC"适用于可观察行为 FR，对纯维护性 FR 可豁免。FR-010 的"验证"通过测试策略隐式覆盖：若测试常量集存在且被其他测试引用，则 FR-010 满足。建议在测试用例表中明确标注 FR-010 由测试策略覆盖，不单独编写 AC。

### A.2 一致性检查

**Goals vs Scope vs FR**：
- Goals 3.1 用户目标（4 项）→ 全部在 Scope 4.1 必须做中找到对应 ✅
- Goals 3.2 业务目标（3 项）→ 全部在 Scope 4.1 必须做中找到对应 ✅
- Goals 3.3 成功标准（6 项）→ 全部在 FR/AC 中找到对应 ✅

**AC vs FR**：
- 10 条 AC 覆盖 11 条 FR 中的 10 条（FR-010 除外，见 A.1）✅
- AC 编号体系 `AC-F1.10-1 ~ AC-F1.10-10` 与 F1.9 的 `AC-F1.9-1 ~ AC-F1.9-12` 编号风格一致 ✅

**Out of Scope vs Scope**：
- Scope 4.1 仅写"必须做"（不写"不包含"）✅ 符合 dd-write-requirements P1 规则
- Out of Scope 第 9 节集中所有"不做"事项，与 Scope 不重复 ✅

**Terminology vs FR**：
- 18 个术语定义在 FR 中使用一致 ✅
- "新默认值"、"旧默认值"、"存储表示"、"用户可见形式"、"键码"、"修饰键"、"快捷键格式化工具"、"快捷键录制器"、"应用设置"、"通用设置视图"、"应用存储"等术语在 FR/AC 中使用一致 ✅

**内部矛盾检查**：
- 未发现 FR 之间互相矛盾 ✅
- 未发现 AC 之间互相矛盾 ✅
- 未发现 Constraints 与 FR 矛盾 ✅

### A.3 docs.md 合规性检查（10 项规则）

| 规则项 | 检查结果 | 备注 |
|--------|---------|------|
| 1. 需求文档章节列表 | ✅ 符合 | 12 章节，与 dd-write-requirements 默认一致，F1.9 也用 12 章节 |
| 2. 设计文档章节列表（预告） | ✅ 待步骤 5a 落地 | 将用 dd-write-design 默认 10 章节 |
| 3. 文档编写顺序 | ✅ 符合 | 需求 → 设计 → 视觉原型 → 测试用例表 |
| 4. 4 篇文档命名规则 | ✅ 符合 | `F1.10_快捷键默认值修改_需求文档.md` |
| 5. 文档存放路径 | ✅ 符合 | `docs/planning/P0/F1/` |
| 6. 版本号和最后更新日期 | ✅ 符合 | `> 最后更新：2026-07-25 | 版本：v1.0` |
| 7. 视觉原型/测试用例表等其他文档 | ✅ 待步骤 5b/5c 落地 | 将产出 HTML 与测试用例表 |
| 8. 标点符号规则 | ✅ 符合 | 中文标点（，。！？：；），英文术语保持原文 |
| 9. mermaid 规则 | ✅ 不适用 | 需求文档无 mermaid 图（与 F1.9 一致） |
| 10. 同步更新规则 | ✅ 不适用 | 当前是新创建文档，未修改既有文档；后续步骤 5 完成后将同步 F1 主规范、F1.9 测试用例表、F1.9 视觉原型 |

### A.4 dd-write-requirements P0 铁律合规性检查

**全文代码符号扫描结果**：

| 扫描模式 | 检查结果 | 备注 |
|---------|---------|------|
| CamelCase 词（类名/协议名/类型名） | ✅ 0 处 | 已修复 `StatusItem` → "菜单栏图标"、`ClipItem` → "剪贴板条目" |
| `xxx()`（方法调用） | ✅ 0 处 | 无 |
| `xxx.yyy`（API 调用/枚举值） | ✅ 0 处 | 无 |
| `protocol`/`func`/`class`/`struct`/`enum` 关键字 | ✅ 0 处 | 无 |
| `.ocr`/`.template`/`.coreML` 等枚举值 | ✅ 0 处 | 无 |
| `idle`/`capturing`/`recognizing` 等英文状态 | ✅ 0 处 | 无 |
| `async let`/`TaskGroup`/`DispatchQueue`/`NSLock` 并发原语 | ✅ 0 处 | 无 |
| `com.xxx.xxx` 配置键名格式 | ✅ 0 处 | 无 |
| `TM_`/`CV_`/`kCG`/`kVK` 等常量前缀 | ✅ 0 处 | 无 |
| 加反引号代码符号 `` `xxx` `` | ⚠️ 仅文件路径 | 文档头部交叉引用路径（如 `docs/planning/P0/F1/...`），与 F1.9 约定一致，属文档导航链接非代码符号 |

**判定**：P0 铁律合规 ✅

### A.5 审查员 A 综合结论

- **状态**：通过（无必须修复问题）
- **建议改进（不阻止通过）**：
  - 在测试用例表中明确标注 FR-010 由测试策略覆盖，不单独编写 AC。
  - 文档头部交叉引用路径可考虑改为相对路径引用（如 `[关联设计文档](./F1.10_快捷键默认值修改_设计文档.md)`），但与 F1.9 风格保持一致亦可。

---

## 审查员 B：可设计性 + 范围 + YAGNI

### B.1 可设计性检查

**逐条 FR 可设计性**：

| FR | 可设计性 | 设计输入是否充分 |
|----|---------|----------------|
| FR-001（默认值改为新值） | ✅ 可设计 | 明确"应用设置、通用设置视图应用存储、快捷键录制器重置按钮三处保持字面一致" |
| FR-002（解析空格键） | ✅ 可设计 | 明确"输入含空格键标识的存储表示 → 输出空格键键码 + 命令 + Shift 修饰键" |
| FR-003（反向构造空格键） | ✅ 可设计 | 明确"输入命令+Shift+空格键码 → 输出新默认值存储表示"，且与解析互逆 |
| FR-004（显示空格键） | ✅ 可设计 | 明确"输入新默认值存储表示 → 输出 ⌘⇧Space"，与字母键大写风格一致 |
| FR-005（老用户迁移） | ✅ 可设计 | 明确"启动时一次检查 + 仅旧默认值迁移 + 不弹对话框 + 日志不记录字面值" |
| FR-006（自定义值保留） | ✅ 可设计 | 明确"非旧默认值不修改 + 空值/无效值按失败路径处理" |
| FR-007（重置按钮） | ✅ 可设计 | 明确"点击后变更为新默认值 + 立即持久化 + 显示更新" |
| FR-008（默认显示） | ✅ 可设计 | 明确"新用户/老用户迁移后/老用户自定义 三种场景的显示行为" |
| FR-009（注册并触发） | ✅ 可设计 | 明确"注册成功 + 按下触发打开快速粘贴面板信号" |
| FR-010（测试常量集） | ✅ 可设计 | 明确"三个常量（default/legacyDefault/arbitrary）+ 位于测试目录 + 不污染生产代码" |
| FR-011（回归保护） | ✅ 可设计 | 明确"旧默认值仍可解析/反向构造/显示" |

**可设计性结论**：所有 FR 都能基于需求文档写出设计文档。需求文档提供了足够的业务行为描述，设计文档可基于此定义模块边界、数据流、状态机。

### B.2 范围检查

**Scope 清晰度**：
- Scope 4.1 列出 7 项"必须做"，每项可独立验收 ✅
- Out of Scope 第 9 节列出 7 项"不做"，与 Scope 不重复 ✅
- 范围边界清晰，无"模糊到可能被两种方式理解"的需求 ✅

**Scope 大小合理性**：
- 本特性是"配置默认值修改 + 解析器扩展 + 老用户迁移"，Scope 大小适中 ✅
- 单 Phase 一次性完成（与上游摘要一致）✅
- 没有过大（如重写整个快捷键系统）或过小（如仅改一个字面值）✅

### B.3 YAGNI 检查（是否加入未请求功能或过度设计）

**对照上游需求摘要 .feature-step0-requirements-summary.md**：

| 上游摘要"必须做"项 | 需求文档对应 | 是否过度 |
|------------------|------------|---------|
| 扩展 HotkeyFormatter 支持 space 键 | FR-002/003/004 | ✅ 无过度 |
| 修改三处默认值 | FR-001 + FR-007 + FR-008 | ✅ 无过度 |
| 老用户迁移逻辑 | FR-005 + FR-006 | ✅ 无过度 |
| 抽取测试常量 TestHotkeys | FR-010 | ✅ 无过度 |
| 测试调整（1 改 + 8 新增 + 5 fixture） | 由测试用例表覆盖 | ✅ 无过度 |
| F1.10 规格文档套件 | 本套件 | ✅ 无过度 |
| F1 主设计规范、F1.9 同步更新 | 由后续步骤覆盖 | ✅ 无过度 |

**对照上游摘要"明确不做"项**：
- 不改快捷键配置 UI → Out of Scope 9.1 ✅
- 不改 F1.9 触发行为 → Out of Scope 9.2 ✅
- 不引入新的快捷键功能 → Out of Scope 9.3 ✅

**YAGNI 检查结论**：
- 所有 FR 都对应上游摘要的"必须做"项 ✅
- 所有 Out of Scope 都对应上游摘要的"明确不做"项 ✅
- 未加入未请求功能（如快捷键冲突检测、快捷键方案、二次 toggle 等）✅
- 未过度设计（如未要求多显示器支持、未要求快捷键使用统计）✅
- Future Considerations 第 12 节列出的扩展项均标注"未来可扩展"，未混入 Scope ✅

### B.4 审查员 B 综合结论

- **状态**：通过（无必须修复问题）
- **可设计性**：所有 FR 可被设计文档实现
- **范围**：清晰、无重复、大小适中
- **YAGNI**：无未请求功能，无过度设计
- **建议改进（不阻止通过）**：无

---

## 审查员 C：可验证性 + UI 可观测性

### C.1 可验证性检查

**AC 给定/当/则格式**：
- 10 条 AC 全部使用 Given/When/Then 三段式 ✅
- 每条 AC 含验证方式（XCTest/XCUITest/手动）✅

**AC 可测试性逐条检查**：

| AC 编号 | 验证方式 | 可测试性 | 备注 |
|---------|---------|---------|------|
| AC-F1.10-1（解析新默认值） | XCTest 新增 | ✅ 可测试 | 输入存储表示，断言解析结果含空格键键码 + 命令 + Shift 修饰键 |
| AC-F1.10-2（显示新默认值） | XCTest 新增 | ✅ 可测试 | 输入存储表示，断言显示结果为 ⌘⇧Space |
| AC-F1.10-3（反向构造新默认值） | XCTest 新增 | ✅ 可测试 | 输入修饰键 + 空格键码，断言反向构造结果为新默认值存储表示 |
| AC-F1.10-4（默认值断言） | XCTest 修改现有 | ✅ 可测试 | 初始化应用设置，断言默认快捷键为新默认值 |
| AC-F1.10-5（老用户迁移） | XCTest 新增 | ✅ 可测试 | 模拟旧默认值持久化，启动应用，断言快捷键迁移为新默认值 |
| AC-F1.10-6（自定义值保留） | XCTest 新增 | ✅ 可测试 | 模拟自定义值持久化，启动应用，断言快捷键保持不变 |
| AC-F1.10-7（重置按钮） | XCTest 新增 | ✅ 可测试 | 触发重置，断言快捷键变更为新默认值并持久化 |
| AC-F1.10-8（注册并触发） | XCTest + 手动 | ✅ 可测试 | XCTest 验证注册成功，手动验证按下触发快速粘贴面板 |
| AC-F1.10-9（默认显示） | XCUITest 新增 | ✅ 可测试 | 打开设置页，断言快捷键录制器显示 ⌘⇧Space |
| AC-F1.10-10（回归保护） | XCTest 保留现有 | ✅ 可测试 | 输入旧默认值存储表示，断言解析结果含字母 V 键码 + 命令 + Shift |

**NFR 可测量性**：

| NFR 编号 | 可测量性 | 备注 |
|---------|---------|------|
| NFR-001（兼容性） | ✅ 可测量 | macOS 13+、arm64/x86_64、现有快捷键注册机制 |
| NFR-002（性能） | ✅ 可测量 | 迁移 ≤ 5ms、注册耗时一致、解析 < 1ms |
| NFR-003（稳定性） | ✅ 可测量 | 100 次连续升级无崩溃、迁移幂等、迁移失败按现有路径 |
| NFR-004（安全性） | ✅ 可测量 | 日志不含快捷键字面值、迁移日志仅元数据 |
| NFR-005（合规性） | ✅ 可测量 | 主 Scheme 合规、无新权限、不暂存到 Dev Scheme |
| NFR-006（可维护性） | ✅ 可测量 | 默认值字面值仅一处、测试常量集集中管理、修改时一处变更 |

### C.2 UI 可观测性检查

**UI 相关 AC 的可观测性**：

| AC 编号 | UI 可观测证据 | 备注 |
|---------|--------------|------|
| AC-F1.10-7（重置按钮） | 快捷键录制器显示更新为 ⌘⇧Space | ✅ UI 可观测（设置页录制器显示变化） |
| AC-F1.10-8（注册并触发） | 快速粘贴面板窗口出现 | ✅ UI 可观测（面板出现是用户可见证据） |
| AC-F1.10-9（默认显示） | 快捷键录制器显示 ⌘⇧Space | ✅ UI 可观测（设置页录制器初始显示） |

**UI 可观测性矩阵完整性**：
- 涉及 UI 的 AC（AC-F1.10-7、AC-F1.10-8、AC-F1.10-9）均明确标注 UI 可见证据 ✅
- 涉及 UI 的 AC 均有 XCUITest 或手动验证方式 ✅
- 不涉及 UI 的 AC（AC-F1.10-1~6、AC-F1.10-10）通过 XCTest 验证模块行为，无需 UI 证据 ✅

**未将内部状态误当成 UI 已验证**：
- AC-F1.10-4（默认值断言）通过 XCTest 验证应用设置的默认值，未误认为 UI 验证 ✅
- AC-F1.10-5（迁移）通过 XCTest 验证持久化值变化，未误认为 UI 验证 ✅
- AC-F1.10-6（自定义值保留）通过 XCTest 验证持久化值不变，未误认为 UI 验证 ✅

### C.3 审查员 C 综合结论

- **状态**：通过（无必须修复问题）
- **可验证性**：所有 AC 用 Given/When/Then，每条 AC 含验证方式，所有 NFR 可测量
- **UI 可观测性**：UI 相关 AC 均有用户可见证据，未将内部状态误当成 UI 已验证
- **建议改进（不阻止通过）**：
  - AC-F1.10-7（重置按钮）可考虑补充 XCUITest 验证方式（除 XCTest 外，可在 UI 层验证重置按钮点击后的显示更新），但当前 XCTest 已覆盖行为，不阻止通过。
  - AC-F1.10-9 当前合并了"新用户首启"与"老用户迁移后"两个场景，可在测试用例表中拆分为两个测试用例（TC）分别验证。

---

## 三审综合结论

### 综合状态

| 审查员 | 维度 | 状态 | 必须修复 | 建议改进 |
|--------|------|------|---------|---------|
| A | 完整性 + 一致性 + docs.md + P0 铁律 | ✅ 通过 | 无 | 2 项（FR-010 AC 标注、交叉引用路径） |
| B | 可设计性 + 范围 + YAGNI | ✅ 通过 | 无 | 无 |
| C | 可验证性 + UI 可观测性 | ✅ 通过 | 无 | 2 项（AC-F1.10-7 补充 XCUITest、AC-F1.10-9 拆分 TC） |

### 是否通过

**通过** ✅

- 三审全通过，无必须修复问题
- 4 项建议改进均不阻止通过，可在测试用例表（步骤 5c）中落地：
  1. 在测试用例表中明确标注 FR-010 由测试策略覆盖（不单独编写 AC）
  2. 在测试用例表中将 AC-F1.10-9 拆分为两个测试用例（新用户首启 + 老用户迁移后）
  3. 在测试用例表中为 AC-F1.10-7 补充 XCUITest 验证方式
  4. 交叉引用路径保持当前风格（与 F1.9 一致）

### 进入步骤 4

需求文档 v1.0 通过 3 维度审查，可进入步骤 4（合并总结 + 一次一问确认）。
