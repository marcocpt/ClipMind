> 最后更新：2026-07-21 | 版本：v1.0

# F2.1 自动保存到文件 测试用例表 - 3 子代理审查结果

**待审查文档**：`docs/planning/P1/F2.1/F2.1_自动保存到文件_测试用例表.md`（v1.0）
**审查时间**：2026-07-21
**审查基线**：`.trae/rules/docs.md` + `F2.1_自动保存到文件_需求文档.md`（v1.0）+ `F2.1_自动保存到文件_设计文档.md`（v1.0）+ `F2.1_自动保存到文件_视觉原型.html`（v1.0）

---

## 审查员 A：完整性 + 一致性 + docs.md 合规性 + AC 覆盖完整性

### A.1 完整性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 文档头部版本号注明对应的需求文档与设计文档版本 | ✅ 通过 | `> 最后更新：2026-07-21 \| 版本：v1.0（基于需求文档 v1.0 + 设计文档 v1.0）` 符合 docs.md 要求 |
| 测试框架说明完整 | ✅ 通过 | XCTest（单元测试）、XCUITest（UI 测试）、手动测试三种框架全部列出，含用途与覆盖范围 |
| AC 覆盖矩阵完整 | ✅ 通过 | 16 条 AC（AC-01 ~ AC-16）全部列出，每条标注验证方式、覆盖状态、对应 FR |
| UI 可观测性矩阵完整 | ✅ 通过 | 10 项 UI AC 全部列出，每项标注对应 AC、真实入口、操作路径、可见结果、证据类型 |
| 测试用例详情完整 | ✅ 通过 | 16 个 AC 驱动的测试用例（TC-AC01-01 ~ TC-AC16-01）+ 33 个单元测试用例（TC-UT-01 ~ TC-UT-33） |
| 每个测试用例包含完整字段 | ✅ 通过 | 测试编号、对应 AC、对应 FR、验证方式、前置条件、测试步骤、预期结果、覆盖状态全部齐全 |
| 覆盖状态汇总完整 | ✅ 通过 | AC 覆盖状态汇总 + 单元测试覆盖状态汇总 + 说明 |
| 测试策略完整 | ✅ 通过 | 单元测试策略 + UI 测试策略 + 手动测试策略 + CI 集成 + 测试覆盖完整性验证 |

### A.2 一致性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| AC 覆盖矩阵与需求文档 AC 一致 | ✅ 通过 | 16 条 AC 编号、场景描述、对应 FR 与需求文档 v1.0 完全一致 |
| 测试用例的对应 FR 与需求文档一致 | ✅ 通过 | 每个测试用例标注的对应 FR 编号在需求文档中存在 |
| 测试用例的前置条件与 AC 一致 | ✅ 通过 | 每个 TC-ACxx-01 的前置条件与对应 AC 的 Given 子句一致 |
| 测试用例的预期结果与 AC 一致 | ✅ 通过 | 每个 TC-ACxx-01 的预期结果与对应 AC 的 Then 子句一致 |
| 单元测试用例与设计文档模块一致 | ✅ 通过 | TC-UT-01 ~ TC-UT-33 覆盖设计文档中的 12 个新增模块（白名单检查器、长度检查器、敏感检查器、文件名生成器、冲突处理器、路径格式化器、文件写入器、自动保存配置模块、状态机） |
| 状态机测试与设计文档状态机一致 | ✅ 通过 | TC-UT-24 ~ TC-UT-33 覆盖设计文档第 5.1 节状态机的所有状态转换 |

### A.3 docs.md 合规性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 文件命名规则 | ✅ 通过 | `F2.1_自动保存到文件_测试用例表.md` 符合 `F{N}_{功能名}_测试用例表.md` |
| 文档存放路径 | ✅ 通过 | `docs/planning/P1/F2.1/` 符合 `planning/P{n}/F{m}/` |
| 文档头部版本号注明对应的设计规范版本 | ✅ 通过 | 注明"基于需求文档 v1.0 + 设计文档 v1.0" |
| 标点符号规则 | ✅ 通过 | 中文标点（，。！？：；）+ 英文术语保持原文（XCTest、XCUITest、Bundle ID 等） |
| 对照代码已有测试标注覆盖状态 | ✅ 通过 | 使用 ✅ COVERED / 🟡 PARTIAL / ❌ MISSING / ⏸️ DEFERRED 四种状态标注（F2.1 尚未实现，全部为 ❌ MISSING） |

### A.4 AC 覆盖完整性检查

| FR 编号 | 对应 AC | 测试用例 | 覆盖状态 |
|---------|---------|---------|---------|
| FR-001 | AC-08 | TC-AC08-01 | ❌ MISSING |
| FR-002 | AC-02, AC-15 | TC-AC02-01, TC-AC15-01 | ❌ MISSING |
| FR-003 | AC-03 | TC-AC03-01 | ❌ MISSING |
| FR-004 | AC-06, AC-14 | TC-AC06-01, TC-AC14-01 | ❌ MISSING |
| FR-005 | AC-01 | TC-AC01-01 | ❌ MISSING |
| FR-006 | AC-10 | TC-AC10-01 | ❌ MISSING |
| FR-007 | AC-04 | TC-AC04-01 | ❌ MISSING |
| FR-008 | AC-01, AC-11 | TC-AC01-01, TC-AC11-01 | ❌ MISSING |
| FR-009 | AC-05, AC-12, AC-13 | TC-AC05-01, TC-AC12-01, TC-AC13-01 | ❌ MISSING |
| FR-010 | AC-07, AC-16 | TC-AC07-01, TC-AC16-01 | ❌ MISSING |
| FR-011 | AC-09, AC-12 | TC-AC09-01, TC-AC12-01 | ❌ MISSING |
| FR-012 | AC-15 | TC-AC15-01 | ❌ MISSING |
| FR-013 | AC-11 | TC-AC11-01 | ❌ MISSING |
| FR-014 | AC-12, AC-13 | TC-AC12-01, TC-AC13-01 | ❌ MISSING |

- ✅ FR-001 ~ FR-014 全部有对应测试用例
- ✅ 每个 AC 至少一个测试用例

### A.5 审查员 A 结论

- **状态**：✅ **通过**
- **必须修复的问题**：无
- **可选改进**：无

---

## 审查员 B：可验证性 + 范围 + YAGNI

### B.1 可验证性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 每个 AC 都有对应的测试用例 | ✅ 通过 | 16 条 AC 全部有对应的 TC-ACxx-01 测试用例 |
| 每个测试用例都有明确的验证方式 | ✅ 通过 | 每个测试用例标注 XCTest / XCUITest / 手动测试 |
| 每个测试用例都有明确的前置条件 | ✅ 通过 | 每个测试用例的 Given 子句转化为前置条件 |
| 每个测试用例都有明确的测试步骤 | ✅ 通过 | 每个测试用例的 When 子句转化为测试步骤 |
| 每个测试用例都有明确的预期结果 | ✅ 通过 | 每个测试用例的 Then 子句转化为预期结果 |
| UI AC 都有用户可见证据 | ✅ 通过 | UI 可观测性矩阵列出 10 项 UI AC 的可见结果与证据类型 |
| 单元测试覆盖所有模块的核心逻辑 | ✅ 通过 | TC-UT-01 ~ TC-UT-33 覆盖 12 个新增模块的核心逻辑 |
| 状态机测试覆盖所有状态转换 | ✅ 通过 | TC-UT-24 ~ TC-UT-33 覆盖状态机的所有状态转换 |

### B.2 范围检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 是否混入非 F2.1 范围的测试 | ✅ 通过 | 所有测试用例都针对 F2.1 的 FR/AC，未混入 F1.x 既有功能的测试 |
| 是否尊重 Out of Scope | ✅ 通过 | 未测试范围外功能（文件编辑、云端、同步、OCR、富文本、自定义模板、分目录、清理、搜索、UI 列表、文件名模板、多目录） |
| 测试用例数量是否合理 | ✅ 通过 | 16 个 AC 驱动测试 + 33 个单元测试 = 49 个测试用例，覆盖 14 条 FR + 12 个模块 + 状态机，数量合理 |

### B.3 YAGNI 检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 是否过早设计未来扩展的测试 | ✅ 通过 | 未测试 FC-01 ~ FC-10 中的未来扩展功能 |
| 测试策略是否过度 | ✅ 通过 | 单元测试覆盖率目标 ≥ 90% 合理；UI 测试覆盖 UI 可观测性矩阵；手动测试仅用于无法自动化的场景 |
| Mock 策略是否合理 | ✅ 通过 | 仅 mock 外部依赖（F1.x 模块、文件系统），不 mock 被测对象本身，符合 CODING_STANDARDS.md 要求 |

### B.4 审查员 B 结论

- **状态**：✅ **通过**
- **必须修复的问题**：无
- **可选改进**：无

---

## 审查员 C：与需求/设计文档一致性 + UI 可观测性

### C.1 与需求文档一致性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| AC 覆盖矩阵与需求文档 AC 完全一致 | ✅ 通过 | 16 条 AC 的编号、场景描述、对应 FR 与需求文档 v1.0 完全一致 |
| 测试用例的预期结果与 AC 的 Then 子句一致 | ✅ 通过 | 每个 TC-ACxx-01 的预期结果都是 AC 的 Then 子句的细化 |
| 验证方式标注符合 NFR-008 要求 | ✅ 通过 | NFR-008 要求"涉及 UI 的 FR 必须有 XCUITest 覆盖；涉及业务逻辑的 FR 必须有 XCTest 覆盖"，本表的验证方式标注符合此要求 |
| 覆盖状态标注规则符合 docs.md | ✅ 通过 | 使用 ✅ COVERED / 🟡 PARTIAL / ❌ MISSING / ⏸️ DEFERRED 四种状态，符合 docs.md 要求 |

### C.2 与设计文档一致性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 单元测试覆盖设计文档的所有模块 | ✅ 通过 | TC-UT-01 ~ TC-UT-33 覆盖设计文档第 2.2 节列出的 12 个新增模块 |
| 状态机测试覆盖设计文档的所有状态转换 | ✅ 通过 | TC-UT-24 ~ TC-UT-33 覆盖设计文档第 5.1 节状态机的 10 个状态与所有转换 |
| 测试策略与设计文档的 NFR 落地一致 | ✅ 通过 | NFR-008（可测试性）落地为"所有模块可通过自动化测试验证，使用临时目录或 mock"，本表的测试策略符合此要求 |

### C.3 UI 可观测性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| UI 可观测性矩阵覆盖所有涉及 UI 的 AC | ✅ 通过 | 10 项 UI AC（AC-01/02/03/05/07/08/09/11/15/16）全部列出 |
| 每项 UI AC 都有用户可见证据 | ✅ 通过 | 每项 UI AC 的"可见结果"列描述了用户可见的证据（文件出现、剪贴板内容、历史列表、配置面板、弹窗等） |
| 每项 UI AC 都有明确的证据类型 | ✅ 通过 | 每项 UI AC 标注 XCUITest + 文件存在性断言 / 剪贴板内容断言 / 历史列表断言 / 弹窗断言等 |
| 证据存放路径明确 | ✅ 通过 | 截图存放在 `docs/planning/P1/F2.1/screenshots/`，录屏存放在 `docs/planning/P1/F2.1/recordings/` |

### C.4 审查员 C 结论

- **状态**：✅ **通过**
- **必须修复的问题**：无
- **可选改进**：无

---

## 综合结论

### 综合状态

- **审查员 A**：✅ 通过（完整性 + 一致性 + docs.md 合规性 + AC 覆盖完整性全部通过）
- **审查员 B**：✅ 通过（可验证性 + 范围 + YAGNI 全部通过）
- **审查员 C**：✅ 通过（与需求/设计文档一致性 + UI 可观测性全部通过）

### 必须修复的问题

无。

### 可选改进

无。

### 是否通过

✅ **通过**，可进入步骤 5c 合并总结 + 一次一问确认。

---

## 版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| v1.0 | 2026-07-21 | 初始版本，3 子代理并行审查 F2.1 测试用例表 v1.0，全部通过，无必须修复问题，无可选改进 |
