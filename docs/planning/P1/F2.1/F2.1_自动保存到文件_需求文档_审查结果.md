> 最后更新：2026-07-21 | 版本：v1.0

# F2.1 自动保存到文件 需求文档 - 3 子代理审查结果

**待审查文档**：`docs/planning/P1/F2.1/F2.1_自动保存到文件_需求文档.md`（v1.0）
**审查时间**：2026-07-21
**审查基线**：`.trae/rules/docs.md` + `docs/CODING_STANDARDS.md` + `docs/planning/P0/F1/F1_ClipMind_设计规范.md` + dd-write-requirements P0 铁律 + dd-write-design P0 铁律

---

## 审查员 A：完整性 + 一致性 + docs.md 合规性 + dd-write-requirements P0 铁律合规性

**规则基准来源**：
- `.trae/rules/docs.md`（项目文档规则）
- `docs/CODING_STANDARDS.md`（项目编码规范）
- dd-write-requirements 默认 12 章节（docs.md 未定义独立"需求文档"章节列表）
- dd-write-design 默认 10 章节（用于步骤 5a 设计文档合规性预检）

### A.1 完整性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 12 章节齐全 | ✅ 通过 | Background / Problem / Goals / Scope / FR / NFR / Constraints / AC / Out of Scope / Terminology / Decision Freedom / Future 全部存在 |
| 无 TODO/占位符/TBD | ✅ 通过 | 全文未出现 TODO、TBD、待定、占位符 |
| 无"优化/改进/更好"等模糊词 | ✅ 通过 | 全文未出现禁用模糊词 |
| 每条 FR 是可观察行为 | ✅ 通过 | 14 条 FR 均描述用户/外部可感知的行为，非内部状态 |
| 每条 NFR 可测量 | ✅ 通过 | 10 条 NFR 均有具体数值或可验证条件（3 秒、500 毫秒、1 秒、100 毫秒等） |
| 每条 AC 用 Given/When/Then | ✅ 通过 | 16 条 AC 全部采用 Given/When/Then 三段式 |
| 每个 FR 至少一个 AC | ✅ 通过 | 14 条 FR 全部有对应 AC（见 A.3 映射表） |
| Decision Freedom 双向 | ✅ 通过 | 10 项允许（A-01~A-10）+ 10 项禁止（F-01~F-10） |
| Terminology 无代码符号 | ✅ 通过 | 13 个术语全部为业务术语，无类名/协议名 |
| Scope 不写"不包含" | ✅ 通过 | Scope 仅写"包含"，所有"不做"条目集中在 Out of Scope |

### A.2 一致性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| FR 与 AC 互相不冲突 | ✅ 通过 | 每条 AC 都对应至少一条 FR，无 AC 描述 FR 未涵盖的行为 |
| NFR 与 AC 互相不冲突 | ✅ 通过 | NFR-001（3 秒）对应 AC-01；NFR-002（500ms）对应 AC-01；NFR-003（1 秒）对应 AC-07；NFR-004（不崩溃）对应 AC-09/AC-12/AC-13 |
| 章节间无内部矛盾 | ✅ 通过 | Background/Goals/Scope/FR/AC 描述一致，无前后矛盾 |
| Out of Scope 与 Scope 不重复 | ✅ 通过 | Scope 第 4.1 节仅写"包含"，Out of Scope 第 9 节是唯一"不做"清单，两节无重复 |
| Constraints 与 FR 不冲突 | ✅ 通过 | C-01~C-12 与 FR-001~FR-014 描述一致，C-07（敏感过滤关闭二次确认）与 FR-004（敏感过滤可关闭）一致 |
| Decision Freedom 禁止项与 Constraints 一致 | ✅ 通过 | F-01~F-10 与 C-09~C-12 描述一致，均要求不修改 F1.x 既有公共接口 |

### A.3 FR 到 AC 映射表（完整性验证）

| FR 编号 | 对应 AC | 覆盖状态 |
|---------|---------|---------|
| FR-001 总开关控制 | AC-08 | ✅ |
| FR-002 白名单 App 触发 | AC-02, AC-15 | ✅ |
| FR-003 内容长度阈值过滤 | AC-03 | ✅ |
| FR-004 敏感内容过滤 | AC-06, AC-14 | ✅ |
| FR-005 文件保存到指定目录 | AC-01 | ✅ |
| FR-006 文件名生成 | AC-10 | ✅ |
| FR-007 文件名冲突处理 | AC-04 | ✅ |
| FR-008 剪贴板替换为文件路径 | AC-01, AC-11 | ✅ |
| FR-009 原内容仍入库 ClipMind 历史 | AC-05, AC-12, AC-13 | ✅ |
| FR-010 配置面板独立分区 | AC-07, AC-16 | ✅ |
| FR-011 保存目录异常处理 | AC-09, AC-12 | ✅ |
| FR-012 白名单 App 管理 | AC-15 | ✅ |
| FR-013 路径格式切换 | AC-11 | ✅ |
| FR-014 自动保存触发时机 | AC-12, AC-13 | ✅ |

### A.4 docs.md 合规性检查（10 项）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 1. 需求文档章节列表 | ✅ 通过 | 使用 dd-write-requirements 默认 12 章节（docs.md 未定义独立"需求文档"章节） |
| 2. 设计文档章节列表（预检） | ✅ 通过 | 步骤 5a 将使用 dd-write-design 默认 10 章节（docs.md 未定义独立"设计文档"章节） |
| 3. 文档编写顺序 | ✅ 通过 | 需求 → 设计 → 视觉原型 → 测试用例表（与任务说明一致） |
| 4. 4 篇文档命名规则 | ✅ 通过 | `F2.1_自动保存到文件_需求文档.md` 符合 `F{N}_{功能名}_需求文档.md` |
| 5. 文档存放路径 | ✅ 通过 | `docs/planning/P1/F2.1/` 符合 `planning/P{n}/F{m}/` |
| 6. 版本号与最后更新日期 | ✅ 通过 | `> 最后更新：2026-07-21 \| 版本：v1.0` 符合格式 |
| 7. 其他文档要求 | ✅ 通过 | 将产出视觉原型（涉及 UI）与测试用例表 |
| 8. 标点符号规则 | ✅ 通过 | 中文标点（，。！？：；）+ 英文术语保持原文（Bundle ID、file://、Markdown 等） |
| 9. mermaid 规则 | ✅ 通过 | 本文档未使用 mermaid（无流程图需求，符合 docs.md"mermaid 用于流程图、状态图、类图"的限定） |
| 10. 同步更新规则 | ✅ 通过 | 版本记录已添加；后续修改时需同步更新测试用例表与视觉原型 |

### A.5 dd-write-requirements P0 铁律检查

**全文搜索代码符号模式**：

| 检查模式 | 命中数 | 状态 |
|---------|--------|------|
| CamelCase 类名（AutoSaveService / FileNameGenerator / FilePathFormatter / AutoSaveSettings / AutoSaveRecord / PasteboardWatcher / ClipCaptureService / AppSettings / AppDetector / SensitiveDetector / EncryptedStore / ClipItem / ClipContent / ContentType / TodoItem / BlacklistEntry / ClipStore / HistoryListView / DetailPanel / SettingsView / LogCategory） | 0 | ✅ 通过 |
| 方法调用 `xxx()` | 0 | ✅ 通过 |
| API 调用 `xxx.yyy()` | 0 | ✅ 通过 |
| 枚举值 `.ocr` / `.template` / `.coreML` | 0 | ✅ 通过 |
| 英文状态枚举 `idle` / `capturing` / `recognizing` / `active` / `emptyResult` / `executing` / `deactivating` | 0 | ✅ 通过 |
| 并发原语 `async let` / `TaskGroup` / `DispatchQueue` / `NSLock` | 0 | ✅ 通过 |
| 配置键名 `com.xxx.xxx` 格式 | 0（`com.apple.Safari` 是 Bundle ID 业务术语示例，非配置键名） | ✅ 通过 |
| 算法常量 `TM_` / `CV_` / `kCG` | 0 | ✅ 通过 |
| 关键字 `protocol` / `func` / `class` / `struct` / `enum` | 0 | ✅ 通过 |
| `UserDefaults` / `SQLite.swift` 等框架 API 或库名 | 0 | ✅ 通过 |

### A.6 审查员 A 结论

- **状态**：✅ **通过**
- **必须修复的问题**：无
- **可选改进**：无

---

## 审查员 B：可设计性 + 范围 + YAGNI

### B.1 可设计性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| FR 是否足够清晰能被 Design 实现 | ✅ 通过 | 每条 FR 描述了具体行为、触发条件、预期结果，可被设计文档转化为模块职责 |
| FR 是否描述了"必须发生什么"而非"由谁负责" | ✅ 通过 | FR 用"系统必须..."描述行为，未指定模块职责（模块职责留给 Design） |
| NFR 是否给了 Design 足够约束 | ✅ 通过 | NFR-001（3 秒）、NFR-006（100ms 额外延迟）等给了性能预算；NFR-010（并发安全）给了并发约束 |
| Constraints 是否仅写业务约束 | ✅ 通过 | C-01~C-08 是业务约束（保存目录、文件格式、路径格式、长度限制等）；C-09~C-12 是兼容性约束（不修改 F1.x 公共接口）；未写技术实现约束（如"必须使用 Actor"） |
| Decision Freedom 是否给了 Design 足够自由度 | ✅ 通过 | A-01~A-10 允许 AI 自由决定模块拆分、文件名过滤规则、冲突检测方式、配置持久化方式等 |
| Terminology 是否建立业务术语表 | ✅ 通过 | 13 个术语建立了业务术语表（自动保存、白名单 App、长度阈值、文件名前缀等），Design 可基于此术语表设计 |

### B.2 范围检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 是否混入多个独立特性 | ✅ 通过 | 单一主题"自动保存到文件"，所有 FR 围绕这一主题展开 |
| 是否与既有 F1.x 功能重复 | ✅ 通过 | F2.1 是 F1.x 之上的扩展（在捕获流程插入钩子），不重复 F1.x 既有功能 |
| Scope 是否清晰界定 | ✅ 通过 | 第 4.1 节明确列出 11 项"本次包含"，第 4.2 节列出 3 项"涉及但不属于本特性单独实现"（复用 F1.x 能力） |
| Out of Scope 是否防止 AI 顺手改 | ✅ 通过 | 12 项 Out of Scope 涵盖文件编辑、云端、同步、OCR、富文本、自定义模板、分目录、清理、搜索、UI 列表、文件名模板、多目录 |

### B.3 YAGNI 检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| FR 数量是否过多 | ✅ 通过 | 14 条 FR 全部围绕核心功能（总开关、白名单、长度、敏感、保存、文件名、冲突、剪贴板、入库、配置面板、异常、白名单管理、路径格式、触发时机），无冗余 |
| AC 数量是否过多 | ✅ 通过 | 16 条 AC 覆盖核心场景（AC-01/02/03）、边界值（AC-04/10）、错误路径（AC-09/12/13）、UI（AC-07/15/16）、持久化（AC-16）、并发健壮性（AC-12/13）、敏感过滤关闭（AC-14），全部必要 |
| NFR 数量是否过多 | ✅ 通过 | 10 条 NFR 涵盖性能、稳定性、隐私、可测试性、兼容性、并发，全部必要 |
| Out of Scope 数量是否过多 | ✅ 通过 | 12 项 Out of Scope 防止 AI 顺手做，合理 |
| Future Considerations 是否过早设计 | ✅ 通过 | 10 项未来扩展明确标注"不在 F2.1 范围内"，仅提示设计时避免堵死扩展路径，不要求实现 |
| Decision Freedom 允许项是否过度授权 | ✅ 通过 | A-01~A-10 授权范围合理（模块拆分、过滤规则、冲突检测、配置持久化、钩子位置、日志分类、UI 布局、错误提示、并发控制、测试组织），未授权修改 F1.x 公共接口 |
| 是否加入未请求功能 | ✅ 通过 | 所有功能均来自上游 `.feature-step0-requirements-summary.md` 已确认的需求摘要，未自行添加新功能 |

### B.4 审查员 B 结论

- **状态**：✅ **通过**
- **必须修复的问题**：无
- **可选改进**：无

---

## 审查员 C：可验证性 + UI 可观测性

### C.1 可验证性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 每条 FR 至少一个 AC | ✅ 通过 | 14 条 FR 全部有对应 AC（见 A.3 映射表） |
| AC 用 Given/When/Then | ✅ 通过 | 16 条 AC 全部采用 Given/When/Then 三段式 |
| AC 的 Then 描述可观察结果 | ✅ 通过 | 所有 AC 的 Then 描述用户可见或测试可断言的结果（文件出现、剪贴板内容、历史条目、弹窗、不崩溃等） |
| AC 标注验证方式 | ⚠️ 部分通过 | AC 未显式标注"XCTest/XCUITest/手动"，但 NFR-008 已要求"涉及 UI 的 FR 必须有 XCUITest 覆盖；涉及业务逻辑的 FR 必须有 XCTest 覆盖"。测试用例表（步骤 5c）将显式标注每条 AC 的验证方式 |
| AC 覆盖边界值 | ✅ 通过 | AC-03（长度不足）、AC-04（文件名冲突）、AC-09（目录异常）、AC-10（特殊字符）、AC-13（入库失败）覆盖边界值 |
| AC 覆盖错误路径 | ✅ 通过 | AC-09（目录不存在）、AC-12（自动保存失败）、AC-13（入库失败）、AC-14（敏感过滤关闭）覆盖错误路径 |
| AC 覆盖并发场景 | ✅ 通过 | AC-12（自动保存失败不影响入库）、AC-13（入库失败不影响自动保存）覆盖并发健壮性 |

### C.2 UI 可观测性检查

| AC 编号 | 是否涉及 UI | 用户可见证据 | 验证方式 |
|---------|------------|-------------|---------|
| AC-01 | 是 | 保存目录出现新文件 + 剪贴板内容变为路径 + ClipMind 历史出现条目 | XCUITest + 文件存在性断言 + 剪贴板内容断言 |
| AC-02 | 是 | 保存目录无新文件 + 剪贴板保持原文 + ClipMind 历史出现条目 | XCUITest + 文件不存在性断言 |
| AC-03 | 是 | 保存目录无新文件 + 剪贴板保持原文 + ClipMind 历史出现条目 | XCUITest + 文件不存在性断言 |
| AC-04 | 是 | 保存目录出现 `content-1.md`、`content-2.md` 等文件 | XCTest + 文件存在性断言 |
| AC-05 | 是 | ClipMind 历史列表顶部出现条目，含来源 App、时间戳、分类标签 | XCUITest + 历史列表断言 |
| AC-06 | 是 | 保存目录无新文件 + 剪贴板保持原文 + ClipMind 历史无条目 | XCTest + 文件不存在性断言 + 历史空断言 |
| AC-07 | 是 | 配置面板修改后 1 秒内生效 + 重启后保留 | XCUITest + 配置持久化断言 |
| AC-08 | 是 | 保存目录无新文件 + 剪贴板保持原文 + ClipMind 行为与 F1.x 一致 | XCUITest + 行为断言 |
| AC-09 | 是 | 弹窗或通知出现 + App 不崩溃 + 剪贴板保持原文 + ClipMind 历史出现条目 | XCUITest + 弹窗断言 + 不崩溃断言 |
| AC-10 | 否 | 文件名包含"你好世界这是测试content"，特殊字符被过滤 | XCTest + 文件名断言 |
| AC-11 | 是 | 剪贴板内容分别为 `file:///...`、`[文件名.md](file:///...)`、`/...` 三种格式 | XCTest + 剪贴板内容断言 |
| AC-12 | 是 | 自动保存失败 + ClipMind 历史仍出现条目 + 剪贴板保持原文 | XCTest + 历史断言 + 剪贴板断言 |
| AC-13 | 是 | 自动保存成功 + 剪贴板变为路径 + ClipMind 历史无条目 + App 不崩溃 | XCTest + 文件断言 + 历史空断言 + 不崩溃断言 |
| AC-14 | 是 | 保存目录出现含 Token 的文件 + 剪贴板变为路径 + ClipMind 历史无条目 | XCTest + 文件内容断言 + 历史空断言 |
| AC-15 | 是 | 白名单列表更新 + 备忘录触发 + Xcode 不触发 | XCUITest + 白名单断言 + 行为断言 |
| AC-16 | 是 | App 重启后配置保留 + 按新配置工作 | XCUITest + 配置持久化断言 |

### C.3 UI AC 是否有用户可见证据

- ✅ 所有涉及 UI 的 AC（AC-01/02/03/05/06/07/08/09/11/15/16）都有用户可见证据（文件出现、剪贴板内容、历史列表、配置面板、弹窗等）
- ✅ 未把内部状态误当成 UI 已验证
- ✅ AC-04/10/12/13/14 虽然主要验证业务逻辑，但也可通过文件系统或剪贴板内容间接验证

### C.4 审查员 C 结论

- **状态**：✅ **通过**
- **必须修复的问题**：无
- **可选改进**：
  - 建议：在测试用例表（步骤 5c）中显式标注每条 AC 的验证方式（XCTest / XCUITest / 手动），与 F1 设计规范 8.2 节 AC 覆盖矩阵风格一致。本审查不阻止通过，由测试用例表承担显式标注责任。

---

## 综合结论

### 综合状态

- **审查员 A**：✅ 通过（完整性 + 一致性 + docs.md 合规性 + P0 铁律合规性全部通过）
- **审查员 B**：✅ 通过（可设计性 + 范围 + YAGNI 全部通过）
- **审查员 C**：✅ 通过（可验证性 + UI 可观测性全部通过，1 项可选改进由测试用例表承担）

### 必须修复的问题

无。

### 可选改进

1. **审查员 C 建议**：在测试用例表（步骤 5c）中显式标注每条 AC 的验证方式（XCTest / XCUITest / 手动），与 F1 设计规范 8.2 节 AC 覆盖矩阵风格一致。本建议不阻止需求文档通过，由测试用例表承担显式标注责任。

### 是否通过

✅ **通过**，可进入步骤 4 合并总结 + 一次一问确认。

---

## 版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| v1.0 | 2026-07-21 | 初始版本，3 子代理并行审查 F2.1 需求文档 v1.0，全部通过，1 项可选改进 |
